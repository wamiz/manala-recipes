##################################################################
# This file is automatically generated when running "manala up". #
##################################################################

version: '3.6'

{{- $has_database := or (.Vars.system.mysql.version) (.Vars.system.mariadb.version) -}}
{{- $has_redis := .Vars.system.redis.version -}}
{{- $has_mailcatcher := .Vars.system.mailcatcher.version -}}

{{ if or ($has_database) ($has_redis) }}

volumes:
    {{ if $has_database -}}
    db-data:
    {{- end }}
    {{ if $has_redis -}}
    redis-data:
    {{- end }}
{{- end }}

services:

{{- if $has_database -}}
{{- $mysql := .Vars.system.mysql }}
{{- $mariadb := .Vars.system.mariadb }}

  database:
    image: {{ if $mysql.version }}'mysql:{{ $mysql.version }}'{{ else }}'mariadb:{{ $mariadb.version }}'{{ end }}
    command: --max_allowed_packet=1024M
    environment:
      MYSQL_USER: 'app'
      MYSQL_PASSWORD: 'app'
      MYSQL_DATABASE: 'app'
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
      TZ: {{ .Vars.system.timezone }}
    volumes:
      - db-data:/var/lib/mysql
      {{- if $mysql.version }}
      - .manala/init-db:/docker-entrypoint-initdb.d
      {{- else }}
      - .manala/config-db/99-additional-config.cnf:/etc/mysql/mariadb.conf.d/99-additional-config.cnf
      {{- end }}
    healthcheck:
      test: mysqladmin ping --silent
      interval: 10s
      timeout: 5s
      retries: 5

{{- end }}

{{- if $has_database }}

  phpmyadmin:
    image: phpmyadmin
    ports: [80]
    environment:
      PMA_HOST: 'database'
      PMA_USER: 'app'
      PMA_PASSWORD: 'app'

{{- end }}

{{- if $has_redis }}

  redis:
    image: 'redis:alpine'
    ports: [6379]
    environment:
      TZ: {{ .Vars.system.timezone }}
    volumes:
      - redis-data:/data

  phpredisadmin:
    image: erikdubbelboer/phpredisadmin
    ports: [80]
    environment:
      REDIS_1_HOST: 'redis'

{{- end }}

{{- if $has_mailcatcher }}

  mailcatcher:
    image: 'schickling/mailcatcher'
    ports: [1025, 1080]
    environment:
      TZ: {{ .Vars.system.timezone }}

{{- end }}

